<UserControl x:Class="MyApp.View.Employees"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:custom="clr-namespace:MyApp.Utilities"
             xmlns:local="clr-namespace:MyApp.View"
             xmlns:vm="clr-namespace:MyApp.ViewModel"
             xmlns:m="clr-namespace:MyApp.Model"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="622"
             >

    <UserControl.DataContext>
        <vm:EmployeeVM />
    </UserControl.DataContext>
    <Grid>

        <Border CornerRadius="0 20 0 20" 
                Background="#212529">

            <Grid>


                <Grid.RowDefinitions>
                    <RowDefinition Height="90"/>
                    <RowDefinition Height="510"/>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>


                <!--Intenstazione + Barra di Ricerca-->
                <Grid Height="50"
                      VerticalAlignment="Top">
                    <Border CornerRadius="0 20 0 0" Margin="0,19,0,-19">
                        <Grid>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="311"/>
                                <ColumnDefinition Width="311"/>
                            </Grid.ColumnDefinitions>
                            <Grid>
                                <TextBlock Style="{StaticResource Text_Style}"
                                           
                                           >Database Dipendenti</TextBlock>
                            </Grid>
                            <Grid Grid.Column="1">

                                <Grid>

                                    <materialDesign:PackIcon Kind="Search"
                                                             Foreground="#dbdbdb"
                                                             VerticalAlignment="Center"
                                                             RenderTransformOrigin="0.5,0.5"
                                                             Margin="34,0,0,0">
                                        <materialDesign:PackIcon.RenderTransform>
                                            <TransformGroup>
                                                <ScaleTransform ScaleX="1.7" ScaleY="1.7"/>
                                                <SkewTransform/>
                                                <RotateTransform/>
                                                <TranslateTransform/>
                                            </TransformGroup>
                                        </materialDesign:PackIcon.RenderTransform>
                                    </materialDesign:PackIcon>

                                    <custom:TxtBox Style="{StaticResource TxtBox_Style}"
                                                   Padding="5 1"
                                                   Text="{Binding SearchString, UpdateSourceTrigger=PropertyChanged}"
                                                   >
                                        <custom:TxtBox.InputBindings>
                                            <KeyBinding Key="Enter" Command="{Binding SearchCommand, UpdateSourceTrigger=PropertyChanged}"/>
                                        </custom:TxtBox.InputBindings>
                                    </custom:TxtBox>
                                </Grid>

                            </Grid>
                        </Grid>

                    </Border>
                </Grid>

                <!--Etichette Record-->
                <Grid Grid.Row="1">
                    <Border BorderBrush="#495057"
                            Width="622"
                            Height="510"
                            BorderThickness="2">
                        <ScrollViewer HorizontalScrollBarVisibility="Auto"
                                      VerticalScrollBarVisibility="Auto">

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto" MinWidth="50"/>
                                    <ColumnDefinition Width="2"/>
                                    <ColumnDefinition Width="auto" MinWidth="50"/>
                                    <ColumnDefinition Width="2"/>
                                    <ColumnDefinition Width="auto" MinWidth="50"/>
                                    <ColumnDefinition Width="2"/>
                                    <ColumnDefinition Width="auto" MinWidth="50"/>
                                    <ColumnDefinition Width="2"/>
                                    <ColumnDefinition Width="auto" MinWidth="50"/>
                                    <ColumnDefinition Width="2"/>
                                    <ColumnDefinition Width="auto" MinWidth="50"/>
                                    <ColumnDefinition Width="2"/>
                                    <ColumnDefinition Width="auto" MinWidth="50"/>
                                    <ColumnDefinition Width="2"/>
                                    <ColumnDefinition Width="auto" MinWidth="50"/>
                                </Grid.ColumnDefinitions>

                                <!--ID_dipendente Record-->
                                <StackPanel Grid.Column="0">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="30"/>
                                            <RowDefinition Height="2"/>
                                            <RowDefinition Height="*"/>


                                        </Grid.RowDefinitions>

                                        <StackPanel Grid.Column="0" Grid.Row="0" 
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Left">
                                            <TextBlock Style="{StaticResource Text_Style}" 
                                                   Height="30"
                                                   Margin="0"
                                                   Padding="10 5"
                                                   TextAlignment="Center"
                                                   >ID Dipendente</TextBlock>
                                        </StackPanel>
                                        <StackPanel Grid.Column="0" Grid.Row="1"
                                                Background="#495057"/>
                                        <Grid Grid.Row="2">
                                            <ListView ItemsSource="{Binding Employees}" Background="#222"
                                                      BorderBrush="#444" 
                                                      Foreground="#dbdbdb"
                                                      Padding="-1"
                                                      ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.CanContentScroll="False" UseLayoutRounding="False" SelectionChanged="ListView_SelectionChanged">
                                                <ListView.Resources>
                                                    <Style TargetType="ListView">
                                                        <Setter Property="BorderThickness"
                                                                Value="0"/>
                                                    </Style>
                                                </ListView.Resources>


                                                <ListView.ItemsPanel>
                                                    <ItemsPanelTemplate>

                                                        <StackPanel Margin="0">

                                                        </StackPanel>
                                                    </ItemsPanelTemplate>
                                                </ListView.ItemsPanel>

                                                <ListView.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid Background="Transparent" Margin="5">
                                                            <TextBlock Text="{Binding Id}" FontSize="14" Style="{StaticResource Text_Style}"
                                                                       Margin="10 0"/>
                                                        </Grid>
                                                    </DataTemplate>
                                                </ListView.ItemTemplate>
                                                <ListView.ItemContainerStyle>
                                                    <Style TargetType="ListViewItem">

                                                        <Setter Property="Background" Value="#333"/>
                                                        <Setter Property="Margin" Value="0"/>
                                                        <Setter Property="Padding" Value="0"/>
                                                        <Setter Property="BorderBrush" Value="#495057"/>
                                                        <Setter Property="BorderThickness" Value="0 0 0 2"/>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#555"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter Property="Background" Value="#777"/>
                                                                <Setter Property="Foreground" Value="White"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ListView.ItemContainerStyle>
                                            </ListView>

                                        </Grid>
                                    </Grid>
                                </StackPanel>

                                <GridSplitter Grid.Column="1" Width="2" 
                                          ResizeBehavior="PreviousAndNext"
                                          HorizontalAlignment="Stretch"
                                          Background="#495057"/>

                                <!--Nome Record-->
                                <StackPanel Grid.Column="2">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="30"/>
                                            <RowDefinition Height="2"/>
                                            <RowDefinition Height="*"/>
                                           
                                        </Grid.RowDefinitions>

                                        <StackPanel Grid.Column="0" Grid.Row="0"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Left">
                                            <TextBlock Style="{StaticResource Text_Style}" 
                                                   Height="30"
                                                   Margin="0"
                                                   Padding="10 5"
                                                   TextAlignment="Center"
                                                    >Nome</TextBlock>
                                        </StackPanel>
                                        <StackPanel Grid.Column="0" Grid.Row="1"
                                                Background="#495057"/>
                                        <Grid Grid.Row="2">
                                            <ListView ItemsSource="{Binding Employees}" Background="#222"
                                                      BorderBrush="#444" 
                                                      Foreground="#dbdbdb"
                                                      Padding="-1"
                                                      ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.CanContentScroll="False" UseLayoutRounding="False" SelectionChanged="ListView_SelectionChanged">
                                                <ListView.Resources>
                                                    <Style TargetType="ListView">
                                                        <Setter Property="BorderThickness"
                                                                Value="0"/>
                                                    </Style>
                                                </ListView.Resources>


                                                <ListView.ItemsPanel>
                                                    <ItemsPanelTemplate>

                                                        <StackPanel Margin="0">

                                                        </StackPanel>
                                                    </ItemsPanelTemplate>
                                                </ListView.ItemsPanel>

                                                <ListView.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid Background="Transparent" Margin="5">
                                                            <TextBlock Text="{Binding Name}" FontSize="14" Style="{StaticResource Text_Style}"
                                                                       Margin="10 0"/>
                                                        </Grid>
                                                    </DataTemplate>
                                                </ListView.ItemTemplate>
                                                <ListView.ItemContainerStyle>
                                                    <Style TargetType="ListViewItem">

                                                        <Setter Property="Background" Value="#333"/>
                                                        <Setter Property="Margin" Value="0"/>
                                                        <Setter Property="Padding" Value="0"/>
                                                        <Setter Property="BorderBrush" Value="#495057"/>
                                                        <Setter Property="BorderThickness" Value="0 0 0 2"/>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#555"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter Property="Background" Value="#777"/>
                                                                <Setter Property="Foreground" Value="White"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ListView.ItemContainerStyle>
                                            </ListView>

                                        </Grid>
                                    </Grid>
                                </StackPanel>
                                

                                <GridSplitter Grid.Column="3" Width="2" 
                                          ResizeBehavior="PreviousAndNext"
                                          Background="#495057"/>

                                <!--Cognome Record-->
                                <StackPanel Grid.Column="4">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="30"/>
                                            <RowDefinition Height="2"/>
                                            <RowDefinition Height="*"/>
                                     
                                        </Grid.RowDefinitions>

                                        <StackPanel Grid.Column="0" Grid.Row="0"
                                                    VerticalAlignment="Center"
                                                    HorizontalAlignment="Left">
                                            <TextBlock Style="{StaticResource Text_Style}" 
                                                   Height="30"
                                                   Margin="0"
                                                   Padding="10 5"
                                                   TextAlignment="Center"
                                            >Cognome</TextBlock>
                                        </StackPanel>
                                        <StackPanel Grid.Column="0" Grid.Row="1"
                                    Background="#495057"/>
                                        <Grid Grid.Row="2">
                                            <ListView ItemsSource="{Binding Employees}" Background="#222"
                                                      BorderBrush="#444" 
                                                      Foreground="#dbdbdb"
                                                      Padding="-1"
                                                      ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.CanContentScroll="False" UseLayoutRounding="False" SelectionChanged="ListView_SelectionChanged">
                                                <ListView.Resources>
                                                    <Style TargetType="ListView">
                                                        <Setter Property="BorderThickness"
                                                                Value="0"/>
                                                    </Style>
                                                </ListView.Resources>


                                                <ListView.ItemsPanel>
                                                    <ItemsPanelTemplate>

                                                        <StackPanel Margin="0">

                                                        </StackPanel>
                                                    </ItemsPanelTemplate>
                                                </ListView.ItemsPanel>

                                                <ListView.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid Background="Transparent" Margin="5">
                                                            <TextBlock Text="{Binding Last}" FontSize="14" Style="{StaticResource Text_Style}"
                                                                       Margin="10 0"/>
                                                        </Grid>
                                                    </DataTemplate>
                                                </ListView.ItemTemplate>
                                                <ListView.ItemContainerStyle>
                                                    <Style TargetType="ListViewItem">

                                                        <Setter Property="Background" Value="#333"/>
                                                        <Setter Property="Margin" Value="0"/>
                                                        <Setter Property="Padding" Value="0"/>
                                                        <Setter Property="BorderBrush" Value="#495057"/>
                                                        <Setter Property="BorderThickness" Value="0 0 0 2"/>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#555"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter Property="Background" Value="#777"/>
                                                                <Setter Property="Foreground" Value="White"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ListView.ItemContainerStyle>
                                            </ListView>

                                        </Grid>
                                    </Grid>
                                </StackPanel>

                                <GridSplitter Grid.Column="5" Width="2" 
                                              ResizeBehavior="PreviousAndNext"
                                              Background="#495057"/>

                                <!--Ruolo Record-->
                                <StackPanel Grid.Column="6">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="30"/>
                                            <RowDefinition Height="2"/>
                                            <RowDefinition Height="*"/>
                                     
                                        </Grid.RowDefinitions>

                                        <StackPanel Grid.Column="0" Grid.Row="0"
                                                    VerticalAlignment="Center"
                                                    HorizontalAlignment="Left">
                                            <TextBlock Style="{StaticResource Text_Style}" 
                                                   Height="30"
                                                   Margin="0"
                                                   Padding="10 5"
                                                   TextAlignment="Center"
                                            >Ruolo</TextBlock>
                                        </StackPanel>
                                        <StackPanel Grid.Column="0" Grid.Row="1"
                                    Background="#495057"/>
                                        <Grid Grid.Row="2">
                                            <ListView ItemsSource="{Binding Employees}" Background="#222"
                                                      BorderBrush="#444" 
                                                      Foreground="#dbdbdb"
                                                      Padding="-1"
                                                      ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.CanContentScroll="False" UseLayoutRounding="False" SelectionChanged="ListView_SelectionChanged">
                                                <ListView.Resources>
                                                    <Style TargetType="ListView">
                                                        <Setter Property="BorderThickness"
                                                                Value="0"/>
                                                    </Style>
                                                </ListView.Resources>


                                                <ListView.ItemsPanel>
                                                    <ItemsPanelTemplate>

                                                        <StackPanel Margin="0">

                                                        </StackPanel>
                                                    </ItemsPanelTemplate>
                                                </ListView.ItemsPanel>

                                                <ListView.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid Background="Transparent" Margin="5">
                                                            <TextBlock Text="{Binding Role}" FontSize="14" Style="{StaticResource Text_Style}"
                                                                       Margin="10 0"/>
                                                        </Grid>
                                                    </DataTemplate>
                                                </ListView.ItemTemplate>
                                                <ListView.ItemContainerStyle>
                                                    <Style TargetType="ListViewItem">

                                                        <Setter Property="Background" Value="#333"/>
                                                        <Setter Property="Margin" Value="0"/>
                                                        <Setter Property="Padding" Value="0"/>
                                                        <Setter Property="BorderBrush" Value="#495057"/>
                                                        <Setter Property="BorderThickness" Value="0 0 0 2"/>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#555"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter Property="Background" Value="#777"/>
                                                                <Setter Property="Foreground" Value="White"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ListView.ItemContainerStyle>
                                            </ListView>

                                        </Grid>
                                        
                                    </Grid>
                                </StackPanel>

                                <GridSplitter Grid.Column="7" Width="2" 
                                              ResizeBehavior="PreviousAndNext"
                                              Background="#495057"/>

                                <!--Reparto Record-->
                                <StackPanel Grid.Column="8">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="30"/>
                                            <RowDefinition Height="2"/>
                                            <RowDefinition Height="*"/>
                                       
                                        </Grid.RowDefinitions>

                                        <StackPanel Grid.Column="0" Grid.Row="0"
                                                    VerticalAlignment="Center"
                                                    HorizontalAlignment="Left">
                                            <TextBlock Style="{StaticResource Text_Style}" 
                                                   Height="30"
                                                   Margin="0"
                                                   Padding="10 5"
                                                   TextAlignment="Center"
                                            >Reparto</TextBlock>
                                        </StackPanel>
                                        <StackPanel Grid.Column="0" Grid.Row="1"
                                    Background="#495057"/>
                                        <Grid Grid.Row="2">
                                            <ListView ItemsSource="{Binding Employees}" Background="#222"
                                                      BorderBrush="#444" 
                                                      Foreground="#dbdbdb"
                                                      Padding="-1"
                                                      ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.CanContentScroll="False" UseLayoutRounding="False" SelectionChanged="ListView_SelectionChanged">
                                                <ListView.Resources>
                                                    <Style TargetType="ListView">
                                                        <Setter Property="BorderThickness"
                                                                Value="0"/>
                                                    </Style>
                                                </ListView.Resources>


                                                <ListView.ItemsPanel>
                                                    <ItemsPanelTemplate>

                                                        <StackPanel Margin="0">

                                                        </StackPanel>
                                                    </ItemsPanelTemplate>
                                                </ListView.ItemsPanel>

                                                <ListView.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid Background="Transparent" Margin="5">
                                                            <TextBlock Text="{Binding Department}" FontSize="14" Style="{StaticResource Text_Style}"
                                                                       Margin="10 0"/>
                                                        </Grid>
                                                    </DataTemplate>
                                                </ListView.ItemTemplate>
                                                <ListView.ItemContainerStyle>
                                                    <Style TargetType="ListViewItem">

                                                        <Setter Property="Background" Value="#333"/>
                                                        <Setter Property="Margin" Value="0"/>
                                                        <Setter Property="Padding" Value="0"/>
                                                        <Setter Property="BorderBrush" Value="#495057"/>
                                                        <Setter Property="BorderThickness" Value="0 0 0 2"/>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#555"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter Property="Background" Value="#777"/>
                                                                <Setter Property="Foreground" Value="White"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ListView.ItemContainerStyle>
                                            </ListView>

                                        </Grid>

                                    </Grid>
                                </StackPanel>

                                <GridSplitter Grid.Column="9" Width="2" 
                                              ResizeBehavior="PreviousAndNext"
                                              Background="#495057"/>

                                <!--Data_Assunzione Record-->
                                <StackPanel Grid.Column="10">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="30"/>
                                            <RowDefinition Height="2"/>
                                            <RowDefinition Height="*"/>
                                     
                                        </Grid.RowDefinitions>

                                        <StackPanel Grid.Column="0" Grid.Row="0"
                                                    VerticalAlignment="Center"
                                                    HorizontalAlignment="Left">
                                            <TextBlock Style="{StaticResource Text_Style}" 
                                                   Height="30"
                                                   Margin="0"
                                                   Padding="10 5"
                                                   TextAlignment="Center"
                                            >Data Assunzione</TextBlock>
                                        </StackPanel>
                                        <StackPanel Grid.Column="0" Grid.Row="1"
                                    Background="#495057"/>

                                        <Grid Grid.Row="2">
                                            <ListView ItemsSource="{Binding Employees}" Background="#222"
                                                      BorderBrush="#444" 
                                                      Foreground="#dbdbdb"
                                                      Padding="-1"
                                                      ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.CanContentScroll="False" UseLayoutRounding="False" SelectionChanged="ListView_SelectionChanged">
                                                <ListView.Resources>
                                                    <Style TargetType="ListView">
                                                        <Setter Property="BorderThickness"
                                                                Value="0"/>
                                                    </Style>
                                                </ListView.Resources>


                                                <ListView.ItemsPanel>
                                                    <ItemsPanelTemplate>

                                                        <StackPanel Margin="0">

                                                        </StackPanel>
                                                    </ItemsPanelTemplate>
                                                </ListView.ItemsPanel>

                                                <ListView.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid Background="Transparent" Margin="5">
                                                            <TextBlock Text="{Binding HireDate}" FontSize="14" Style="{StaticResource Text_Style}"
                                                                       Margin="10 0"/>
                                                        </Grid>
                                                    </DataTemplate>
                                                </ListView.ItemTemplate>
                                                <ListView.ItemContainerStyle>
                                                    <Style TargetType="ListViewItem">

                                                        <Setter Property="Background" Value="#333"/>
                                                        <Setter Property="Margin" Value="0"/>
                                                        <Setter Property="Padding" Value="0"/>
                                                        <Setter Property="BorderBrush" Value="#495057"/>
                                                        <Setter Property="BorderThickness" Value="0 0 0 2"/>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#555"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter Property="Background" Value="#777"/>
                                                                <Setter Property="Foreground" Value="White"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ListView.ItemContainerStyle>
                                            </ListView>

                                        </Grid>
                                    </Grid>
                                </StackPanel>

                                <GridSplitter Grid.Column="11" Width="2" 
                                              ResizeBehavior="PreviousAndNext"
                                              Background="#495057"/>

                                <!--Data_Assunzione Record-->
                                <StackPanel Grid.Column="12">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="30"/>
                                            <RowDefinition Height="2"/>
                                            <RowDefinition Height="*"/>
                                 
                                        </Grid.RowDefinitions>

                                        <StackPanel Grid.Column="0" Grid.Row="0"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Left">
                                            <TextBlock Style="{StaticResource Text_Style}" 
                                                 Height="30"
                                                 Margin="0"
                                                 Padding="10 5"
                                                 TextAlignment="Center"
                                          >Data Nascita</TextBlock>
                                        </StackPanel>
                                        <StackPanel Grid.Column="0" Grid.Row="1"
                                  Background="#495057"/>
                                        <Grid Grid.Row="2">
                                            <ListView ItemsSource="{Binding Employees}" Background="#222"
                                                      BorderBrush="#444" 
                                                      Foreground="#dbdbdb"
                                                      Padding="-1"
                                                      ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.CanContentScroll="False" UseLayoutRounding="False" SelectionChanged="ListView_SelectionChanged">
                                                <ListView.Resources>
                                                    <Style TargetType="ListView">
                                                        <Setter Property="BorderThickness"
                                                                Value="0"/>
                                                    </Style>
                                                </ListView.Resources>


                                                <ListView.ItemsPanel>
                                                    <ItemsPanelTemplate>

                                                        <StackPanel Margin="0">

                                                        </StackPanel>
                                                    </ItemsPanelTemplate>
                                                </ListView.ItemsPanel>

                                                <ListView.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid Background="Transparent" Margin="5">
                                                            <TextBlock Text="{Binding BirthDate}" FontSize="14" Style="{StaticResource Text_Style}"
                                                                       Margin="10 0"/>
                                                        </Grid>
                                                    </DataTemplate>
                                                </ListView.ItemTemplate>
                                                <ListView.ItemContainerStyle>
                                                    <Style TargetType="ListViewItem">

                                                        <Setter Property="Background" Value="#333"/>
                                                        <Setter Property="Margin" Value="0"/>
                                                        <Setter Property="Padding" Value="0"/>
                                                        <Setter Property="BorderBrush" Value="#495057"/>
                                                        <Setter Property="BorderThickness" Value="0 0 0 2"/>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#555"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter Property="Background" Value="#777"/>
                                                                <Setter Property="Foreground" Value="White"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ListView.ItemContainerStyle>
                                            </ListView>

                                        </Grid>
                                    </Grid>
                                </StackPanel>

                                <GridSplitter Grid.Column="13" Width="2" 
                                            ResizeBehavior="PreviousAndNext"
                                            Background="#495057"/>

                                <!--Stipendio Record-->
                                <StackPanel Grid.Column="14">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="30"/>
                                            <RowDefinition Height="2"/>
                                            <RowDefinition Height="*"/>
                                  
                                        </Grid.RowDefinitions>

                                        <StackPanel Grid.Column="0" Grid.Row="0"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Left">
                                            <TextBlock Style="{StaticResource Text_Style}" 
                                                 Height="30"
                                                 Margin="0"
                                                 Padding="10 5"
                                                 TextAlignment="Center"
                                          >Stipendio</TextBlock>
                                        </StackPanel>
                                        <StackPanel Grid.Column="0" Grid.Row="1"
                                                    Background="#495057"/>

                                        <Grid Grid.Row="2">
                                            <ListView ItemsSource="{Binding Employees}" Background="#222"
                                                      BorderBrush="#444" 
                                                      Foreground="#dbdbdb"
                                                      Padding="-1"
                                                      ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.CanContentScroll="False" UseLayoutRounding="False" SelectionChanged="ListView_SelectionChanged">
                                                <ListView.Resources>
                                                    <Style TargetType="ListView">
                                                        <Setter Property="BorderThickness"
                                                                Value="0"/>
                                                    </Style>
                                                </ListView.Resources>


                                                <ListView.ItemsPanel>
                                                    <ItemsPanelTemplate>

                                                        <StackPanel Margin="0">

                                                        </StackPanel>
                                                    </ItemsPanelTemplate>
                                                </ListView.ItemsPanel>

                                                <ListView.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid Background="Transparent" Margin="5">
                                                            <TextBlock Text="{Binding Salary}" FontSize="14" Style="{StaticResource Text_Style}"
                                                                       Margin="10 0"/>
                                                        </Grid>
                                                    </DataTemplate>
                                                </ListView.ItemTemplate>
                                                <ListView.ItemContainerStyle>
                                                    <Style TargetType="ListViewItem">

                                                        <Setter Property="Background" Value="#333"/>
                                                        <Setter Property="Margin" Value="0"/>
                                                        <Setter Property="Padding" Value="0"/>
                                                        <Setter Property="BorderBrush" Value="#495057"/>
                                                        <Setter Property="BorderThickness" Value="0 0 0 2"/>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#555"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter Property="Background" Value="#777"/>
                                                                <Setter Property="Foreground" Value="White"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ListView.ItemContainerStyle>
                                            </ListView>

                                        </Grid>
                                    </Grid>
                                </StackPanel>

                                <GridSplitter Grid.Column="15" Width="2" 
                                            ResizeBehavior="PreviousAndNext"
                                            Background="#495057"/>




                            </Grid>

                        </ScrollViewer>
                    </Border>
                </Grid>

            </Grid>
        </Border>
    </Grid>
</UserControl>
